name: Check state of other required PRs

on:
  pull_request:
    branches: [master, main, release/**]

jobs:
  check-required-prs:
    uses: NTNewHorizons/NTNH-Actions-Workflows/.github/workflows/gate-by-required.yml@master
    secrets: inherit
